# next-auth beta.32 declares peerOptional nodemailer<=8, but <=9.0.0 is the
# vulnerable range (GHSA-c7w3 / GHSA-vvjj) and we run our own transporter in
# src/lib/email.ts (next-auth's email provider is unused) — so resolve legacy.
legacy-peer-deps=true
